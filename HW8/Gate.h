#ifndef GATE_H
#define GATE_H

#include <iostream>
#include <string>

class Gate {
	private:
		string type;
		int delay;
		Wire input1;
		Wire input2;
		Wire output;
	public:
		Gate(string type, Gate )
};